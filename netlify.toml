[build]
  command = "corepack enable && pnpm install && pnpm run build"
  publish = "dist/public"

[build.environment]
  NODE_VERSION = "22"

# Serve pre-rendered HTML files first, fallback to index.html for SPA routes
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
